# -*- coding: utf-8 -*-
# vim: ft=yaml
---
dehydrated:
  pkg:
    name: dehydrated
  rootgroup: root
  config: '/etc/dehydrated'
  service:
    name: dehydrated
  subcomponent:
    config: '/etc/dehydrated-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
